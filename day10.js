function showPrompt() {
    var name = prompt('What is your name?');
    var age = prompt('How old are you?');
    if (name, age !== null) {
        console.log('Hey ' + name +
         ' how are you? and how do you feel being ' + age + ' years old?');
    }
}
